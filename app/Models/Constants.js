/**
 * constants used in identify the car model
 * @type {{SUV: number, SEDAN: number, FOURxFOUR: number}}
 */
const rateCodes = {
    SUV: 2,
    SEDAN: 0,
    FOURxFOUR: 1
};

module.exports = rateCodes;